{% extends 'base.html.jinja' %}

{% block content %}
    <section>
        <div class="container">
            <h1>ConPLex</h1>

            <p>
                {% if g.user %}
                    Welcome, <em>{{ g.user.email_address }}</em>.
                {% else %}
                    Welcome.
                {% endif %}
            </p>
        </div>
    </section>
{% endblock %}
